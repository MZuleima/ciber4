apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation

on:
  push:
    branches:
      - '**'

jobs:
  build:
    steps:
      - name: Checkout Repository
        uses: docker://golang:1.20.3-alpine3.17
        with:
          provider: github
          repository: MZuleima/ciber4
          token: secrets.MI_TOKEN_GITHUB
          run: |
            ls -al
