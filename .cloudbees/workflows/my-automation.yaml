apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation

on:
  push:
    branches:
      - '**'
jobs:
  build:
    steps:
      - name: Checkout Repository
        uses: cloudbees/checkout@v2
        with:
          repository: MZuleima/ciber4
          token: ${{ secrets.MI_TOKEN_GITHUB }}

        run: |
          ls -al
